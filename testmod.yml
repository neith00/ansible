- name: test my new module
  hosts: localhost
  tasks:
  - name: run the new module
    ignite:
      name: 'hello'
      new: true
    register: testout
  - name: dump test output
    debug:
      msg: '{{ testout }}'
